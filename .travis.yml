language: java
sudo: false # faster builds

script: "mvn cobertura:cobertura"

jdk:
  - oraclejdk8
script: mvn clean install

after_success:
  - bash <(curl -s https://codecov.io/bash)

deploy:
  provider: pages
  skip_cleanup: true
  github_token: 80f1527a23cee761268e65389aa8aeb5d1fc2bf5
  local_dir: target/cucumber/cucumber-html-report
  on:
    branch: master
