language: java
sudo: false # faster builds

script: "mvn cobertura:cobertura"

jdk:
  - oraclejdk8
script: mvn clean install

after_success:
  - bash <(curl -s https://codecov.io/bash)

deploy:
  provider: pages
  skip_cleanup: true
  github_token: 5e49083ca134f19cb518385c4cea185fa08ec966
  local_dir: target/cucumber/cucumber-html-report
  on:
    branch: master
