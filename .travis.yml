language: java
sudo: false # faster builds

script: "mvn cobertura:cobertura"

jdk:
  - oraclejdk8
script: mvn clean install

after_success:
  - bash <(curl -s https://codecov.io/bash)

deploy:
  provider: pages
  skip_cleanup: true
  github_token: 6c95909f067370d02027c04a3eb4ce17520f91ca
  local_dir: target/cucumber/cucumber-html-report
  on:
    branch: master
